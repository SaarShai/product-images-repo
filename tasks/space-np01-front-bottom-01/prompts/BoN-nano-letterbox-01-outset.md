The 9:16 frame is INTENTIONALLY wider than the panel: render the panel as a tall narrow strip centered with WHITE side margins (as in image 1). Do not stretch the panel to fill the 9:16 width.

Produce ONE finished tall, narrow watercolor space control-panel illustration.

CANVAS & FRAMING (critical): the panel is a TALL, NARROW vertical strip — its height is about 3.6x its width. Image 1 shows the EXACT layout contract: the panel sits CENTERED on the canvas with empty WHITE MARGINS on the left and right. Reproduce that framing EXACTLY: do NOT enlarge the panel to fill the frame, do NOT crop the margins, do NOT change the panel's tall-narrow proportions. The white side margins must remain white and empty.

LAYOUT CONTRACT (image 1) — treat as law, not suggestion. Keep every element at the IDENTICAL relative position, size, and shape:
- the outer silhouette and its edges, including the small V-notch in the top edge,
- ONE long, slightly tapered, rounded vertical opening in the upper-middle (a tall narrow slot that gently narrows toward its lower end),
- ONE small round circular opening (a port hole) positioned in the middle, a short distance BELOW the long slot.

OPENINGS — OVERSIZED EMPTY KEEP-CLEAR (the critical instruction): each opening must be surrounded by a GENEROUS empty paper-white margin that is clearly LARGER than the opening itself — leave a clean empty band of roughly 12–18 panel-points (about 4–6% of the panel width) of untouched paper around the FULL perimeter of every opening. Treat each opening as if it were bigger than drawn: keep ALL hardware, paint, controls, outlines, screws and texture entirely OUTSIDE this enlarged keep-clear band. Nothing may touch or approach the opening edge — the empty zone is intentionally too big, so the eventual cut always falls inside empty paper even if placement drifts a few points. The recessed CENTER of each opening stays empty and pale (paper-white aperture). Paint a soft bevelled rim only at the OUTER edge of the enlarged keep-clear band, never tight against the aperture.

STYLE — the OTHER attached image(s) are the STYLE REFERENCE and your single source of truth for the look. Match them precisely:
- their BRIGHTNESS and palette: a BRIGHT, luminous mid-cobalt blue body with clean white paper breathing through and soft top-left light — NOT a dark navy, muddy, or saturated slab,
- their COLORFUL control hardware: capsule buttons and toggles in RED, GOLDEN-YELLOW, GREEN and TEAL; cream/off-white rounded KNOBS with thin dark rims and short white RADIAL TICK MARKS around them; small sliders, indicator dots, corner screws,
- their crisp hand-painted dark-blue ink outlines (not murky blooms), granulated watercolor paper texture, and clean white background.
Do NOT invent a darker, muddier, monochrome, cobalt-and-indigo, or navy-slab palette — copy the reference images' bright, colorful look exactly. Add the raised rounded control hardware ONLY in the open body areas, clear of both openings (and their enlarged keep-clear bands) and the outer edge.

DO NOT: move, resize, rotate, reshape, merge, add, remove, fill, close, or round any opening; add new openings; add text, labels, arrows, grid lines, or watermark. One single finished panel, white background, one version only.

Use the other attached images as the watercolor style reference (palette, brightness, colorful controls, brushwork, bevel/rim treatment) — match their look, but ignore their LAYOUT entirely (the layout comes only from image 1).
