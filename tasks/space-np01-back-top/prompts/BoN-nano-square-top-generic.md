Image 1 is INTENTIONALLY on a wider canvas than the panel: render the panel centered with WHITE margins around it (as in image 1). Do not stretch the panel to fill the canvas.

Produce ONE finished watercolor space control-panel illustration.

CANVAS & FRAMING (critical): the panel is a roughly SQUARE, irregularly-shaped plate (NOT a tall strip). Image 1 shows the EXACT layout contract: the panel sits CENTERED on the canvas with empty WHITE MARGINS around it. Reproduce that framing EXACTLY: do NOT enlarge the panel to fill the frame, do NOT crop the margins, do NOT change the panel's proportions or silhouette. The white margins must remain white and empty.

LAYOUT CONTRACT (image 1) — treat as LAW. Image 1 is the single source of truth for GEOMETRY:
- Reproduce the outer silhouette and ALL its edges EXACTLY, including the angled/stepped edges, the slanted top, and any notches or tabs along the bottom and sides.
- Reproduce EVERY opening shown in image 1 at the IDENTICAL position, size, shape, and orientation — same count, same places (e.g. a diagonal rounded slot and a circular port). Do NOT add, remove, move, resize, rotate, or reshape any opening.
- The grey shapes in image 1 mark the EMPTY openings (cut-through ports), drawn a little larger than tight — keep that full empty area empty.

OPENINGS — painted recessed ports with bevelled rims (NOT flat holes): around EACH opening paint an illustrated bevelled rim — a soft dark-navy inner shadow on the upper-left edge and a pale lit lip on the lower-right edge, with watercolor pigment pooling along the rim. Leave the recessed CENTER empty and pale (paper-white aperture). Keep a generous empty paper margin around each opening (as shown by the grey area in image 1); keep ALL hardware, paint, outlines, screws, and texture OUTSIDE that empty area.

STYLE — the OTHER attached image(s) are the STYLE REFERENCE and your single source of truth for the LOOK. Match them precisely:
- BRIGHTNESS and palette: a BRIGHT, luminous mid-cobalt blue body with clean white paper breathing through and soft top-left light — NOT a dark navy, muddy, or saturated slab,
- COLORFUL control hardware: capsule buttons and toggles in RED, GOLDEN-YELLOW, GREEN and TEAL; cream/off-white rounded KNOBS with thin dark rims and short white RADIAL TICK MARKS around them; small sliders, indicator dots, corner screws,
- crisp hand-painted dark-blue ink outlines (not murky blooms), granulated watercolor paper texture, clean white background.
Do NOT invent a darker, muddier, monochrome, or navy-slab palette — copy the reference images' bright, colorful look exactly. Add the raised rounded control hardware ONLY in the open body areas, clear of every opening and the outer edge.

DO NOT: move, resize, rotate, reshape, merge, add, remove, fill, close, or round any opening; add new openings; add text, labels, arrows, grid lines, or watermark. One single finished panel, white background, one version only.

Use the other attached images as the watercolor style reference only — match their look, but ignore their LAYOUT entirely (the layout comes only from image 1).
