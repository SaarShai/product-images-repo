# Task-retrospective report

## Task
- Goal: Learn from Cursor “Chimney bricks + tree cutout” and Codex “Explore gingerbread style options”; persist durable project lessons.
- Future trigger: Festive gingerbread cutout decoration, soft-interior fal clarity upscale/detail batches, masked vs unmasked cutout deliverables.
- Definition of done: ≤3 gated durable writes with read-back; no skill sprawl unless procedure-shaped and uncovered.
- Evidence quality: **High** (handoff packet digests of both sessions + existing gates/wiki). Missing: raw image byte re-verification (paths cited only).

## What happened
- Key steps: Built handoff packet → decided write set → Fable 5 high advisor review (once) → amended → patched DIAGNOSIS + wiki + CORRECTION-GATE → wiki index + search read-back.
- Verification evidence: `wiki.py search "magenta fringes"` / `"biscuit-brick"` / `"unmask"` hit updated pages; DIAGNOSIS contains `PROBE ONLY` + WARNING gate; index `status: ok` (26 pages).
- User corrections (banked): Stage C batch magenta/neon rejection; don’t heal creative fails; chimney biscuit bricks; tree unmask-first; unmasked artwork deliverable. Edge-v4/Styled V1 lock already in peppermint overlay prompts/loop-spec.

## Reusable learnings
1. Lesson: High-creativity fal clarity candy-creative (`creat ~0.65`) is probe-only; single “amazing” probe ≠ batch approval; do not heal failed outputs — regenerate from original via Stage A (default) ± approved Stage B.
   Applies when: festive soft-interior clarity upscale / “add interesting candy detail” after 8×.
   Trigger/symptom: magenta fringes or neon-green blobs after creative clarity; or treating one creative probe as batch recipe.
   Evidence: Cursor U20–U22, U25–U26; stale DIAGNOSIS “rolled to 01–07” corrected.
   Target: `DIAGNOSIS.md` + wiki upscale concept trigger bullet.
   Write-gate: PASSED (`error`, `user_confirmed`, `this-repo`, score 9.5).
   Action: **persisted**.

2. Lesson: Chimney slots take biscuit-brick *texture* (not scenes); when cutouts look masked/cropped, unmask/restore geometry-matched art before redesign; deliver unmasked artwork when user needs Illustrator pieces.
   Applies when: festive gingerbread chimney/tree cutouts; “masked/cropped” complaints; AB5-style handoff.
   Trigger/symptom: dense candy in chimney slots; truncated cutout graphics; user asks for artwork without mask.
   Evidence: Cursor U05–U08; Codex C11; edge-v8 approval.
   Target: decoration-slots wiki + `CORRECTION-GATE.md`.
   Write-gate: PASSED (`error`, `user_confirmed`, `this-repo`, score 8.0).
   Action: **persisted**.

3. Lesson: Edge-v4 base + Styled V1 motif lock.
   Target: drop — already in `tasks/festive-edge-v4-peppermint-overlay/` prompts + loop-spec gate.
   Action: **dropped** (Fable-confirmed).

## Rejected learnings
- Candidate: New `/learn` skill for festive clarity batch.
  Reason rejected: route-probe FACT-shaped; DIAGNOSIS holds recipe; wiki upscale page already says “Why Not A Skill Yet”; Fable endorsed no-skill.
- Candidate: Always-on AGENTS.md/CLAUDE.md.
  Reason rejected: not repo-wide enough; task/wiki targets suffice.
- Candidate: Live Illustrator geometry (Codex C12).
  Reason rejected: one-off; Fable accepted leave unbanked.
- Candidate: Re-bank decoration-slots “no houses” / styled-candidate-proof-gate.
  Reason rejected: already banked.

## Project updates
- File/page updated: `tasks/festive-v1-gingerbread-candidates/outputs/upscale-research/DIAGNOSIS.md`
  Read-back: Stage C header `PROBE ONLY`; WARNING gate present; “rolled to 01–07” removed.
- File/page updated: `wiki/concepts/illustrated-product-upscale-and-background-removal-workflow.md`
  Read-back: `wiki.py search "magenta fringes"` → this page; dates `2026-07-09`.
- File/page updated: `wiki/concepts/gingerbread-panel-cutouts-decoration-slots.md`
  Read-back: `wiki.py search "biscuit-brick"` / `"unmask"` → this page; chimney/unmask/deliverable rules + exemplars.
- File/page updated: `tasks/festive-v1-gingerbread-candidates/CORRECTION-GATE.md`
  Read-back: Chimney / tree / deliverable gates section present.
- File/page updated: `wiki/log.md` entry for this retrospective.
- Advisor: Fable 5 high — one call, conclusions only.

## Remaining risks
- DIAGNOSIS is task-local; agents that only read the Marine Bed Real-ESRGAN wiki SOP may miss fal clarity Stage A/B/C unless they hit the new trigger bullet.
- Tree “green redesign then restore” sequence is banked as unmask-first; future agents may still over-interpret a green-tree ask — exemplar is there.
- No new proposed skill; if clarity batching recurs on another product folder, reconsider `/learn` then.
