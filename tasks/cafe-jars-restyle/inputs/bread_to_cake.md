Replace each BREAD item in this image with a CAKE.

There are four baked-goods shapes in a row (a pretzel, a baguette, and two loaves
of bread). Turn each one into a different appetizing CAKE — for example a slice of
layer cake, a round frosted cake, a bundt cake, a small cupcake. One cake per
existing item.

HARD CONSTRAINTS:
- Keep the EXACT SAME dimensions, canvas size, and the SAME four positions / slots.
  Each cake must sit in the same place and occupy roughly the same footprint as the
  bread it replaces. Do NOT add or remove items, do NOT move them, do NOT resize the
  canvas, do NOT re-crop or zoom.
- Keep the EXACT SAME art style: flat 2D vector, solid color fills, NO outlines,
  NO gradients, NO texture, strictly monochrome warm brown-on-cream palette (tan,
  caramel, coffee-brown, dark espresso, cream). No other colors.
- Background stays pure solid WHITE (#FFFFFF), plain.

Change ONLY the identity bread -> cake. Same style, same layout, same dimensions.
